Pre-release security hardening and crash-fix sweep

- Fix FreeRADIUS clients.conf to use $ENV{RADIUS_SECRET} syntax; pass RADIUS_SECRET via docker-compose
- Remove hardcoded JWT fallback secrets across auth modules and WireGuard service
- Add startup validation for critical env vars (JWT_SECRET, DATABASE_URL) in main.ts
- Null-safe token parsing in API interceptor to prevent crash during refresh
- Safe list cast in dashboard bloc to handle null/non-list router responses
- Null fallbacks for id/email in user_model to prevent crash on malformed JSON
- Add session authorization (filter by userId) across sessions controller/service
- Fix revenue aggregation query in routers service
- Remove empty-password RADIUS login bypass in FreeRADIUS default site
- Add XSS sanitization in admin VoucherList, JWT expiry check in admin App
- Add input validation DTOs for all admin endpoints
- Add IP validation for router DTOs, file size check for payment uploads
- Add connectivity checks, retry with backoff, and graceful degradation in dashboard
- Add 40+ localization keys (EN/AR) for error messages and UI strings
- Add Nginx security headers and rate limiting
- Fix Dockerfile CMD path, move @prisma/client to dependencies
- Replace force-unwraps with null-safe alternatives across mobile UI pages
